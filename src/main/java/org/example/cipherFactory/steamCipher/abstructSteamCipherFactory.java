package org.example.cipherFactory.steamCipher;

import org.example.cipherBean;
import org.example.cipherFactory.abstructCipherFactory;

public abstract class abstructSteamCipherFactory extends abstructCipherFactory {
    public abstructSteamCipherFactory(cipherBean config) throws Exception {
        super(config);
    }
}
