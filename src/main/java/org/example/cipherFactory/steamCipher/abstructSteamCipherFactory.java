package org.example.cipherFactory.steamCipher;

import org.example.cipherBean;
import org.example.cipherFactory.absCipherFactory;

public abstract class abstructSteamCipherFactory extends absCipherFactory {
    public abstructSteamCipherFactory(cipherBean config) throws Exception {
        super(config);
    }
}
